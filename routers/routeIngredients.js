const { Router } = require('express');
const controller = require('../controllers/controllerIngredients');

const router = Router();

// router.get('/', controller.getIngredients);
// router.post('/', controller.addIngredients);


module.exports = router;